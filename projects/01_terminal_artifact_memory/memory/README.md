# Verified memory workspace

`wiki/` contains human-reviewed Markdown pages admitted by `lily.memory`. `manifests/artifact_index.jsonl` is created on first admission. The empty directories are retained with `.gitkeep` files.

Do not place raw trajectories, hidden tests, reference solutions, verifier internals, credentials, private infrastructure details, or unreviewed summaries here. Follow [`../OPERATOR.md`](../OPERATOR.md) for the mandatory admission gates.
